import styled from "styled-components";

export const InputStyle = styled.div`
  display: flex;
  flex-flow: column;
  margin: 1rem 0;
  label {
    margin-bottom: 0.5rem;
    font-weight: bold;
  }
  input {
    padding: 0.5rem;
  }
`;
